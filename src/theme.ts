import { createTheme } from '@mui/material'

const theme = createTheme({
  palette: {
    primary: {
      main: '#293f2b',
    },
    secondary: {
      main: '#ffd700',
    },
  },
})

export default theme
